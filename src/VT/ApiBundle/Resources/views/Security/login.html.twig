<script type="text/javascript">
    if (typeof currentUrl === 'undefined')
    {
        window.location = '/#login';
    }
</script>
<br>
{% if error %}
    <div class="alert alert-danger">{{ error.message }}</div>
{% endif %}
<div class="top-content">
            
            <div class="inner-bg">
                <div class="container">
                    <div class="row">
                        <div class="col-md-4 col-sm-4 hero-feature">
                            <div class="thumbnail">
                                <video width="300" height="250" controls="controls">
                                  <source src="{{ asset('bundles/api/video/chaine.mp4') }}" type="video/mp4" />
                                  <source src="{{ asset('bundles/api/video/chaine.webm') }}" type="video/webm" />
                                  <source src="{{ asset('bundles/api/video/chaine.ogv') }}" type="video/ogg" />
                                </video>
                            </div>
                        </div>
                        <div class="col-sm-5 col-sm-offset-3 form-box">
                            <div class="form-top">
                                <div class="form-top-left">
                                    <h3>Connection à platin</h3>
                                    <p>Entrer votre email et mot de passe :</p>
                                </div>
                                <div class="form-top-right">
                                    <i class="fa fa-lock"></i>
                                </div>
                            </div>
                            <div class="form-bottom">
                                <form role="form" action="{{ path('login_check') }}" method="post" class="login-form">
                                    <div class="form-group">
                                        <label class="sr-only" for="form-username">Email</label>
                                        <input type="text" name="_username" placeholder="{{ 'form.email.label'|trans({}, 'messages', user_locale) }}" class="form-username form-control" id="form-username">
                                    </div>
                                    <div class="form-group">
                                        <label class="sr-only" for="form-password">Mot de passe</label>
                                        <input type="password" name="_password" placeholder="{{ 'form.password.label'|trans({}, 'messages', user_locale) }}" class="form-password form-control" id="form-password">
                                    </div>
                                    <button type="submit" class="btn">{{ 'button.connection.label'|trans({}, 'messages', user_locale) }}</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
        </div>
