<input type="hidden" id="idsujet" value="{{ id }}"/>
<h4>Sujet – {{subject}} ({% if countContribution < 2 %} {{countContribution}} contribution {% else %} {{countContribution}} contributions {% endif %})</h4>
<br />
				<table>
					<div class="well">{{consigne}}</div>
				</table>
				<br />
				<table>
					<tr>
						<td>
							<a class="btn btn-danger" title="Retourner à la page des sujets" href="{{ path('innova_platin_page_forum')}}">Retour</a> 
						</td>
					</tr>
				</table>
				<table>
					<tr>
						<td>
							<a class="open-AddBookDialog btn btn-danger" data-toggle="modal" href="#" data-id="0" data-target="#Record" >+</a>
						</td>
					</tr>
				</table>
				<br />
				<table class="table table-striped table-bordered">
					<tr>
						<th style="width: 10%; text-align: center;"></th>
						<th style="width: 30%; text-align: center;"></th>
						<th style="width: 60%; text-align: center;"></th>
					</tr>
					{% set compteur = 0 %}
					{% for sons in listeContributions %}
					<tr  id="monAncre">
						<td>

								<img id="record" src="{{ asset('bundles/InnovaPlatin/css/bootstrap/images/video_icon.png') }}" >
						</td>
						<td>
							<div>
								<span style="font-weight: bold; color: #1290D0;">Déposé par {{sons.user}}</span><br />
								<span style="font-size: x-small;">Le {{sons.date|date("d/m/Y")}} à {{sons.time|date("H:i:s")}}</span>
								<br />
							</div>
							<br />
						</td>
						<td>
								  <video src="{{asset(sons.contents)}}" id="audioBalise_{{sons.id}}" controls="controls"></video></span><br />
						</td>
					</tr>
					{% set compteur = compteur + 1 %}
					{% endfor %}

				</table>
{% endblock %}